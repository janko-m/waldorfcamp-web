!!!
%html{:lang => "en"}
  %head
    %title Waldorf Camp &middot; #{@title}
    %meta{:charset => "utf-8"}
    %script{:src => 'http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js'}
    %link{:rel => "authors", :href => "humans.txt"}
    %link{:rel => 'stylesheet', :type => 'text/css', :href => 'http://fonts.googleapis.com/css?family=Love+Ya+Like+A+Sister|Gentium+Basic:400,700,400italic,700italic'}
    = favicon_link_tag
    = stylesheet_link_tag "application", "jquery.fancybox"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body{:id => @title.downcase}
    %nav.primary
      %ol
        = list_of navigation_pages do |page|
          = link_to_unless_current page.title, :action => page.action do
            %span.active= page.title

    %nav.secondary
      = content_for :navigation

    %header#header
      %hgroup
        %h1#logo= link_to "Waldorf Camp", index_path
        %h2#tagline An Active and Dynamic Vacation

    %div{:role => 'main'}
      = yield
