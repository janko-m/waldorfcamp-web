- @title = @workshop.title

- content_for :navigation do
  %ul
    = list_of @workshops do |workshop|
      = link_to_unless workshop == @workshop, workshop, workshop_path(workshop) do
        %span.active= workshop

%article
  = render_markdown(@workshop.description)
